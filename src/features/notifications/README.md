# Feature: notifications

But: centraliser la logique des notifications (hooks, service FCM, UI modales).

- hooks/: useUserNotifications, useFCM
- services/: abonnement/stockage notifications
- components/: NotificationsModal, badges
- utils/: formatage, mapping statuts

Note: structure créée sans déplacement de code pour l’instant.
